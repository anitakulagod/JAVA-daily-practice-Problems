//Write a Java program to count the number of vowels and consonants in a given string.
//1.The program should ignore case (i.e., treat uppercase and lowercase letters the same).
//2.Spaces, digits, and special characters should be ignored while counting.

//*Test Case 01 :*
//Input : Data Science 123!
//Output : 
//Number of vowels: 5  
//Number of consonants: 6

//*Test Case 02 :*

//Input : Beautiful Day
//Output : 
//Number of vowels: 6  
//Number of consonants: 6

import java.util.Scanner;

public class VowelsAndConsonants {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input string
        System.out.print("Enter a string: ");
        String str = sc.nextLine();

        int vowels = 0, consonants = 0;

        // Convert string to lowercase for case-insensitive comparison
        str = str.toLowerCase();

        // Count vowels and consonants
        for (int i = 0; i < str.length(); i++) {
            char ch = str.charAt(i);

            if (ch >= 'a' && ch <= 'z') { // Only letters
                if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
                    vowels++;
                } else {
                    consonants++;
                }
            }
        }

        // Output
        System.out.println("Number of vowels: " + vowels);
        System.out.println("Number of consonants: " + consonants);

        sc.close();
    }
}
