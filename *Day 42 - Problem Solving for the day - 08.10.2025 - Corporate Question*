//Write a Java program to count how many positive, negative, and zero elements are present in an array.The program should read the array size and its elements from the user, and then display the count of positive, negative, and zero values.
//*Test Case 01 :*
//Input : 
//5
//1 -2 0 4 -6
//Output :
//Positive: 2
//Negative: 2
//Zero: 1
//*Test Case 02 :*
//Input :
//6
//0 0 -1 2 -3 4
//Output :
//Positive: 2
//Negative: 2
//Zero: 2

import java.util.Scanner;

public class CountPosNegZero {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input size
        int n = sc.nextInt();

        int[] arr = new int[n];

        // Counters
        int positive = 0, negative = 0, zero = 0;

        // Input array elements and count
        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();

            if (arr[i] > 0) {
                positive++;
            } else if (arr[i] < 0) {
                negative++;
            } else {
                zero++;
            }
        }

        // Output
        System.out.println("Positive: " + positive);
        System.out.println("Negative: " + negative);
        System.out.println("Zero: " + zero);

        sc.close();
    }
}
