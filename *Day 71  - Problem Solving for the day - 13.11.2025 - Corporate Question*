//You are given two strings s1 and s2.Write a function to return True if any permutation of s1 is present as a substring of s2, otherwise return False.
In other words, check if s2 contains a substring that is an anagram of s1.
 *Test Case 01 :* 
Input:
s1 = "ab"
s2 = "eidbaooo"
Output:
True
 *Test Case 02 :* 
Input:
s1 = "adc"
s2 = "dcda"
Output:
True

import java.util.Scanner;

public class PermutationInString {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        // Input strings
        System.out.print("Enter s1: ");
        String s1 = sc.next();

        System.out.print("Enter s2: ");
        String s2 = sc.next();

        // If s1 is longer than s2, impossible
        if (s1.length() > s2.length()) {
            System.out.println("False");
            return;
        }

        int[] freq1 = new int[26];
        int[] freq2 = new int[26];

        // Frequency of s1
        for (int i = 0; i < s1.length(); i++) {
            freq1[s1.charAt(i) - 'a']++;
            freq2[s2.charAt(i) - 'a']++;
        }

        // Check first window
        if (isSame(freq1, freq2)) {
            System.out.println("True");
            return;
        }

        // Sliding window
        for (int i = s1.length(); i < s2.length(); i++) {
            freq2[s2.charAt(i) - 'a']++;                  // Add new char
            freq2[s2.charAt(i - s1.length()) - 'a']--;   // Remove old char

            if (isSame(freq1, freq2)) {
                System.out.println("True");
                return;
