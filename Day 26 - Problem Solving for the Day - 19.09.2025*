//Given two integers M and N, perform the following tasks:
Find the sum of squares of all integers from M to N (inclusive).If the sum is greater than N, return -1.
*Otherwise:*
If the sum is even, return the sum divided by 2.
If the sum is odd, return the sum as it is.
*Test Case 01 :*
Input :
M = 1, N = 5
Output :
-1
*Test Case 02 :*
Input :
M = 0, N = 1
Output :
1

import java.util.Scanner;

public class SumOfSquares {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        System.out.print("Enter M: ");
        int M = sc.nextInt();

        System.out.print("Enter N: ");
        int N = sc.nextInt();

        int sum = 0;

        // Calculate sum of squares from M to N
        for (int i = M; i <= N; i++) {
            sum += i * i;
        }

        // Apply given conditions
        if (sum > N) {
            System.out.println(-1);
        } else {
            if (sum % 2 == 0) {
                System.out.println(sum / 2);
            } else {
                System.out.println(sum);
            }
        }

        sc.close();
    }
}
