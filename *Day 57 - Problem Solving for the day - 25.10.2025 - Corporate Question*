//Write a Java program to convert the first letter of each word in a given string to uppercase.
 - The program should handle multiple spaces between words correctly.
 - All other letters in each word should remain in lowercase.
 - Ignore punctuation when identifying word boundaries.
*Test Case 01 :*
Input :
welcome to the corporate world
Output :
Welcome To The Corporate World
*Test Case 02 :*
Input :
   multiple   spaces    before and after    
Output :
Multiple Spaces Before And After

import java.util.Scanner;

public class CapitalizeWords {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        System.out.println("Enter a sentence:");
        String input = sc.nextLine();

        input = input.toLowerCase(); // convert entire string to lowercase
        StringBuilder result = new StringBuilder();

        boolean capitalizeNext = true;

        for (int i = 0; i < input.length(); i++) {
            char ch = input.charAt(i);

            if (Character.isLetter(ch)) {
                if (capitalizeNext) {
                    result.append(Character.toUpperCase(ch));
                    capitalizeNext = false;
                } else {
                    result.append(ch);
                }
            } else {
                // space or punctuation
                result.append(ch);
                capitalizeNext = true;
            }
        }

        System.out.println(result.toString().trim());

        sc.close();
    }
}
