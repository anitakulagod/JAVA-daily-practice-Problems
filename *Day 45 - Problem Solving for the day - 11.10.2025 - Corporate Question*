//Write a Java program that reads an integer from the user and creates a new number where each digit of the original number is replaced by its square.Finally, display the resulting number.
//*Test Case 01 :*
//Input : 234
//Output :4916
//*Test Case 02 :*
//Input : 105
//Output : 1025

import java.util.Scanner;

public class SquareOfDigits {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input
        System.out.print("Enter a number: ");
        int n = sc.nextInt();

        // Convert number to string (ignore sign)
        String num = String.valueOf(Math.abs(n));
        String result = "";

        // Process each digit from left to right
        for (int i = 0; i < num.length(); i++) {
            int digit = num.charAt(i) - '0';
            result = result + (digit * digit);
        }

        // Output
        System.out.println(result);

        sc.close();
    }
}
