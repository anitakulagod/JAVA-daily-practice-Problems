//You are given an integer array arr[] and an integer K. Your task is to count the number of pairs (arr[i], arr[j]) such that:
i < j
arr[i] + arr[j] = K
Return the total number of valid pairs.
*Test Case 01 :*
Input: arr = {1, 5, 7, -1, 5} , K = 6
Output: 3
*Test Case 02 :*
Input: arr = {2, 2, 2, 2} , K = 4
Output: 6

import java.util.HashMap;
import java.util.Scanner;

public class CountPairsWithSumK {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        // Input array size
        System.out.print("Enter array size: ");
        int n = sc.nextInt();

        int[] arr = new int[n];

        // Input array elements
        System.out.println("Enter array elements:");
        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }

        // Input K value
        System.out.print("Enter value of K: ");
        int K = sc.nextInt();

        HashMap<Integer, Integer> map = new HashMap<>();
        int count = 0;

        // Count pairs
        for (int num : arr) {
            int required = K - num;

            if (map.containsKey(required)) {
                count += map.get(required);
            }

            // Update frequency
            map.put(num, map.getOrDefault(num, 0) + 1);
        }

        // Output result
        System.out.println("Number of valid pairs: " + count);
    }
}
