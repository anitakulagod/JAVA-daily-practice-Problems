//Write a Java program to find and print the ASCII value of each character in a given string.
//- The program should take a string as input.
 //- For each character in the string, print its corresponding ASCII value.
 //- Ignore spaces while printing the ASCII values.
//*Test Case 01 :*
//Input :
//String: "ABC"

//Output :
//A: 65  
//B: 66  
//C: 67

//*Test Case 02 :*
//Input :
//String: "Hi"
//Output :
//H: 72  
//i: 105

import java.util.Scanner;

public class PrintASCIIValues {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input string
        System.out.print("Enter a string: ");
        String str = sc.nextLine();

        // Process each character
        for (int i = 0; i < str.length(); i++) {
            char ch = str.charAt(i);

            if (ch != ' ') { // Ignore spaces
                System.out.println(ch + ": " + (int) ch);
            }
        }

        sc.close();
    }
}
