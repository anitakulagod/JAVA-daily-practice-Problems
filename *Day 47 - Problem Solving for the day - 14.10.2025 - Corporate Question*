//Write a Java program that reads a positive integer and checks whether its digits are in strictly increasing order from left to right.Digits are strictly increasing if every digit is smaller than the next digit.
//If the condition is satisfied, print "Digits are in increasing order". Otherwise, print "Digits are not in increasing order".
//*Test Case 01 :*
//Input : 12345
//Output : Digits are in increasing order
//*Test Case 02 :*
//Input: 12435
//Output : Digits are not in increasing order

import java.util.Scanner;

public class StrictlyIncreasingDigits {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input
        System.out.print("Enter a positive number: ");
        long n = sc.nextLong();

        String num = String.valueOf(n);
        boolean isIncreasing = true;

        // Check digits from left to right
        for (int i = 0; i < num.length() - 1; i++) {
            if (num.charAt(i) >= num.charAt(i + 1)) {
                isIncreasing = false;
                break;
            }
        }

        // Output
        if (isIncreasing) {
            System.out.println("Digits are in increasing order");
        } else {
            System.out.println("Digits are not in increasing order");
        }

        sc.close();
    }
}
