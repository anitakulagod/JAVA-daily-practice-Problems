//You are given an integer array arr of size N. Your task is to find the first repeating element in the array.
First repeating element means: An element that appears more than once and whose first occurrence index is smallest among all repeating elements.
If no element repeats, print -1.
*Test Case 01 :*
Input :
N = 7
arr = [10, 5, 3, 4, 3, 5, 6]
Output : 5
*Test Case 02 :*
Input :
N = 5
arr = [1, 2, 3, 4, 5]
Output :-1
*Test Case 03 :*
Input :
N = 6
arr = [7, 1, 2, 1, 3, 2]
Output :1

import java.util.*;

public class FirstRepeatingElement {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        // Read size of array
        int N = sc.nextInt();

        int[] arr = new int[N];

        // Read array elements
        for (int i = 0; i < N; i++) {
            arr[i] = sc.nextInt();
        }

        HashSet<Integer> set = new HashSet<>();
        int result = -1;

        // Traverse from right to left
        for (int i = N - 1; i >= 0; i--) {
            if (set.contains(arr[i])) {
                result = arr[i];
            } else {
                set.add(arr[i]);
            }
        }

        // Output result
        System.out.println(result);

        sc.close();
    }
}
